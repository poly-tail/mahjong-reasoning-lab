# Optional fallback only.
# Prefer `python -m naga_ptev.cli store-login` if you do not want plaintext
# credentials on disk. Copy to `.env` only if you accept that tradeoff.

NAGA_NICONICO_MAIL_TEL=your_niconico_mail_or_tel
NAGA_NICONICO_PASSWORD=your_niconico_password
